{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "cd ../.. && pnpm --filter @workspace/db build && pnpm --filter @workspace/auth build && pnpm --filter api build && pnpm --filter mcp-server build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
